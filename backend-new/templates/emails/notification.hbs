<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notification</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            width: 100% !important;
            min-width: 100%;
            background-color: #f4f4f4;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            line-height: 1.6;
            color: #333333;
        }

        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .email-header {
            background: linear-gradient(135deg, #6f42c1 0%, #e83e8c 100%);
            color: #ffffff;
            padding: 30px;
            text-align: center;
        }

        .email-header.info {
            background: linear-gradient(135deg, #17a2b8 0%, #6610f2 100%);
        }

        .email-header.warning {
            background: linear-gradient(135deg, #ffc107 0%, #fd7e14 100%);
        }

        .email-header.success {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
        }

        .email-header.error {
            background: linear-gradient(135deg, #dc3545 0%, #e83e8c 100%);
        }

        .email-header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }

        .email-content {
            padding: 40px 30px;
        }

        .email-content h2 {
            color: #333333;
            font-size: 24px;
            margin: 0 0 20px 0;
            font-weight: 600;
        }

        .email-content p {
            margin: 0 0 15px 0;
            font-size: 16px;
            line-height: 1.6;
        }

        .button {
            display: inline-block;
            background: linear-gradient(45deg, #2196F3, #21CBF3);
            color: #ffffff !important;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 16px;
            margin: 20px 0;
        }

        .button-center {
            text-align: center;
            margin: 30px 0;
        }

        .notification-box {
            background: #f8f9fa;
            border-left: 4px solid #2196F3;
            padding: 15px 20px;
            margin: 20px 0;
            border-radius: 0 6px 6px 0;
        }

        .notification-box.info {
            border-left-color: #17a2b8;
            background: #d1ecf1;
        }

        .notification-box.warning {
            border-left-color: #ffc107;
            background: #fff3cd;
        }

        .notification-box.success {
            border-left-color: #28a745;
            background: #d4edda;
        }

        .notification-box.error {
            border-left-color: #dc3545;
            background: #f8d7da;
        }

        .email-footer {
            background: #f8f9fa;
            padding: 25px 30px;
            text-align: center;
            border-top: 1px solid #e9ecef;
            font-size: 13px;
            color: #666666;
        }

        @media screen and (max-width: 600px) {
            .email-container {
                width: 100% !important;
                margin: 0 !important;
                border-radius: 0 !important;
            }
            
            .email-header,
            .email-content,
            .email-footer {
                padding: 20px !important;
            }

            .button {
                display: block !important;
                width: 100% !important;
                box-sizing: border-box;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="email-header {{#if type}}{{type}}{{/if}}">
            <h1>{{#if icon}}{{icon}}{{else}}🔔{{/if}} {{title}}</h1>
            {{#if subtitle}}
            <p>{{subtitle}}</p>
            {{/if}}
        </div>
        
        <div class="email-content">
            {{#if greeting}}
            <h2>{{greeting}}</h2>
            {{/if}}
            
            <p>{{message}}</p>

            {{#if details}}
            <div class="notification-box {{#if type}}{{type}}{{/if}}">
                <p><strong>Details:</strong></p>
                {{#if details.list}}
                <ul>
                    {{#each details.list}}
                    <li>{{this}}</li>
                    {{/each}}
                </ul>
                {{else}}
                <p>{{details}}</p>
                {{/if}}
            </div>
            {{/if}}

            {{#if actionUrl}}
            <div class="button-center">
                <a href="{{actionUrl}}" class="button">{{#if actionText}}{{actionText}}{{else}}Take Action{{/if}}</a>
            </div>
            {{/if}}

            {{#if additionalInfo}}
            <p>{{additionalInfo}}</p>
            {{/if}}

            {{#if timestamp}}
            <p><small><strong>Time:</strong> {{timestamp}}</small></p>
            {{/if}}
        </div>
        
        <div class="email-footer">
            <p>{{#if footerText}}{{footerText}}{{else}}You received this notification because of your account settings.{{/if}}</p>
            {{#if companyName}}
            <p>&copy; {{currentYear}} {{companyName}}. All rights reserved.</p>
            {{/if}}
        </div>
    </div>
</body>
</html>